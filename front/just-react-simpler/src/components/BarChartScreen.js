import BarChart from "./BarChart";

export default function BarChartScreen({ width = 500, height = 300 }) {
    return (
        <div className='bar-chart-screen'>
            <BarChart />
        </div>
    )
}